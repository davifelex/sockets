{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/server/sources/*.cpp",
                //"${workspaceFolder}/client/sources/*.cpp",
                "-o",
                "${workspaceFolder}/server/bin/${fileBasenameNoExtension}"
                //"${workspaceFolder}/client/bin/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
